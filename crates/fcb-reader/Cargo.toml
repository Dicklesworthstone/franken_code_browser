[package]
name = "fcb-reader"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false
description = "Exact source reading lens: line numbers, wrapping, find-in-file, pending far lines (FCB-019.A)."

[workspace]

[dependencies]
fcb-core = { path = "../fcb-core" }
