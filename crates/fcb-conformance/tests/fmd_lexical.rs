//! Relocated: the real-engine hostile campaign lives in the
//! franken_markdown repository (tests/resume_hostile_adversarial.rs), which
//! owns the FCB-021 engine. This crate stays dependency-free; see
//! fcb-d5xi.1's receipts for the relocation rationale.

#[test]
fn real_engine_adapter_relocated_to_upstream_tests() {
    // Marker test keeping this file a valid target after the relocation.
}
